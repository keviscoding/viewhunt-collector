document.addEventListener("DOMContentLoaded", function() { const maxCards = document.querySelectorAll(".pricing-card"); maxCards.forEach(function(card) { const title = card.querySelector("h3"); if (title && title.textContent.trim() === "Max") { const list = card.querySelector(".features-list"); if (list) { const proFeature = document.createElement("li"); proFeature.innerHTML = "<i class=\"fas fa-check\"></i> Everything in Pro plan"; list.insertBefore(proFeature, list.firstChild); } } }); const landingMaxCard = Array.from(document.querySelectorAll(".pricing-card")).find(function(card) { const title = card.querySelector(".pricing-title"); return title && title.textContent.trim() === "Max"; }); if (landingMaxCard) { const featuresContainer = landingMaxCard.querySelector(".pricing-features"); if (featuresContainer) { const proFeature = document.createElement("div"); proFeature.className = "pricing-feature"; proFeature.innerHTML = "<span class=\"pricing-check\">✓</span><span>Everything in Pro plan</span>"; featuresContainer.insertBefore(proFeature, featuresContainer.firstChild); } } });
